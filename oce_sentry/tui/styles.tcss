Screen {
    layout: vertical;
}

#body {
    height: 1fr;
}

/* The title column is what an OCE reads first, so the table gets the room.
   The detail pane is a fixed width rather than a fraction: at 2fr it took 40%
   of the terminal and squeezed titles down to about eight characters. */
#incidents {
    width: 1fr;
    height: 100%;
}

#side {
    width: 42;
    min-width: 36;
    height: 100%;
    border-left: solid $panel;
    /* Reserve the scrollbar column so long lines keep their last word. */
    scrollbar-gutter: stable;
    padding: 1 2;
}

/* Below roughly 100 columns the pane costs more than it gives: the incident
   column collapses to two characters and the queue stops being readable. */
Screen.-narrow #side {
    display: none;
}

#detail {
    height: auto;
    width: 1fr;
}

/* Make the cursor row unmistakable: the detail pane follows it, and on a dense
   table it is otherwise easy to lose which incident you are reading. */
DataTable > .datatable--cursor {
    background: $accent;
    color: $text;
    text-style: bold;
}

DataTable {
    scrollbar-size-vertical: 1;
    scrollbar-size-horizontal: 1;
}

#status {
    height: 1;
    padding: 0 1;
    background: $panel;
    color: $text-muted;
}

ConfirmRun {
    align: center middle;
}

#confirm-box {
    width: 90;
    height: auto;
    padding: 1 2;
    border: thick $warning;
    background: $surface;
}

#confirm-detail {
    height: auto;
}

#sli-body {
    height: 1fr;
}

#sli-table {
    height: auto;
    max-height: 40%;
}

#sli-detail {
    height: 1fr;
    border-top: solid $panel;
}

#sli-summary {
    width: 1fr;
    padding: 1 2;
}

#sli-breakdown {
    width: 1fr;
    border-left: solid $panel;
}

#sli-status {
    height: 1;
    padding: 0 1;
    background: $panel;
    color: $text-muted;
}

#bug-body {
    height: 1fr;
}

#bug-table {
    height: 1fr;
}

#bug-detail {
    height: auto;
    min-height: 12;
    padding: 1 2;
    border-top: solid $panel;
}

#bug-status {
    height: 1;
    padding: 0 1;
    background: $panel;
    color: $text-muted;
}

CreateBugScreen {
    align: center middle;
}

/* The form scrolls and the buttons are pinned. Previously the buttons were
   laid out below the content, which put them off-screen on a 26-row terminal:
   the one screen where a human types something had an invisible submit. */
#bug-form {
    width: 96;
    max-width: 96%;
    height: 90%;
    padding: 1 2;
    border: thick $accent;
    background: $surface;
    layout: vertical;
}

#bug-scroll {
    height: 1fr;
    overflow-y: auto;
    /* Reserve the scrollbar column so it cannot overlay the form. */
    scrollbar-gutter: stable;
}

#bug-note {
    height: 7;
    margin: 1 0;
}

#bug-preview {
    height: auto;
    padding: 1 0;
}

#bug-form-buttons {
    height: 3;
    dock: bottom;
    margin-top: 1;
}

#bug-form-buttons Button {
    margin-right: 2;
}

#bug-form-status {
    height: auto;
    max-height: 2;
}


#lib-body {
    height: 1fr;
}

#lib-table {
    height: auto;
    max-height: 50%;
}

/* One full-width pane below the table: the highlighted skill while browsing,
   the run output while running. The old right-hand log was only ever used
   during a run and cost half the width the rest of the time. */
#lib-detail {
    height: 1fr;
    border-top: solid $panel;
    /* Without a stable gutter the scrollbar overlays the last two columns
       and long lines lose their final word -- text that simply vanishes
       rather than wrapping. */
    scrollbar-gutter: stable;
    padding: 1 2;
}

#lib-summary {
    width: 1fr;
}

#lib-status {
    height: 1;
    padding: 0 1;
    background: $panel;
    color: $text-muted;
}


/* Skill browser: the filter box only exists while filtering, so it takes no
   vertical space from the table until it is shown. */
#lib-filter {
    height: 3;
    margin: 0 1;
}

/* Kits view. Same shape as the skill browser on purpose: the two screens are
   siblings and moving between them should not move anything on screen. */
#kit-body {
    height: 1fr;
}

#kit-table {
    height: auto;
    max-height: 45%;
}

/* Same single-pane shape as the skill browser. */
#kit-detail {
    height: 1fr;
    border-top: solid $panel;
    scrollbar-gutter: stable;
    padding: 1 2;
}

#kit-summary {
    width: 1fr;
}

#kit-status {
    height: 1;
    padding: 0 1;
    background: $panel;
    color: $text-muted;
}

/* Settings. The summary sits above the table because "connectors are not
   wired into skill runs" is the answer most visits are looking for, and it
   should not require selecting a row. */
#set-body {
    height: 1fr;
}

#set-summary {
    height: auto;
    padding: 1 2;
    background: $panel;
}

#set-table {
    height: auto;
    max-height: 45%;
}

/* One full-width detail pane. The old right-hand log was empty most of the
   time and cost half the width of the only part carrying real detail --
   access requirements do not fit in sixty columns. */
#set-detail {
    height: 1fr;
    border-top: solid $panel;
    scrollbar-gutter: stable;
    padding: 1 2;
}

#set-info {
    width: 1fr;
}

#set-status {
    height: 1;
    padding: 0 1;
    background: $panel;
    color: $text-muted;
}


/* Result of a run. The body deliberately does NOT wrap: a query result is a
   table, column alignment is the only thing that makes it readable, and
   re-flowing it to the pane width is what made results look choppy. */
#result-head {
    height: auto;
    padding: 1 2;
    background: $panel;
}

#result-body {
    height: 1fr;
    padding: 1 2;
    overflow-x: auto;
    scrollbar-gutter: stable;
}

#result-text {
    width: auto;
}

#result-status {
    height: 1;
    padding: 0 1;
    background: $panel;
    color: $text-muted;
}

/* Compose. The path sits on its own line at the bottom because it is the
   product: it is what the operator hands their agent, so it is visible before,
   during and after the build. */
#cmp-head {
    height: auto;
    padding: 1 2;
    background: $panel;
}

#cmp-body {
    height: 1fr;
}

#cmp-table {
    height: auto;
    max-height: 50%;
}

#cmp-detail {
    height: 1fr;
    border-top: solid $panel;
    scrollbar-gutter: stable;
    padding: 1 2;
}

#cmp-info {
    width: 1fr;
}

#cmp-path {
    height: 1;
    padding: 0 1;
    background: $accent;
    color: $text;
    text-style: bold;
}
